/**
 * Точка входа дочернего процесса плагина.
 *
 * Отдельным файлом, потому что `fork` требует путь к скрипту, а не функцию.
 * Всё содержательное — в host.ts; здесь только запуск, чтобы файл можно было
 * собрать отдельной точкой в бандл и найти на диске в обоих режимах.
 */
import { startPluginHost } from './host.js';

startPluginHost();
