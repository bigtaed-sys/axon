/**
 * Точка входа процесса плагина в собранном пакете.
 *
 * Существует отдельно от `cli.ts` и `index.ts` по прозаичной причине: `fork`
 * принимает путь к файлу, а не функцию. В npm-пакете рядом с бандлом должен
 * лежать файл, который можно запустить, — вот он.
 */
import { startPluginHost } from '@axon/core';

startPluginHost();
