/** Подставляется Vite при сборке — см. define в vite.config.ts. */
declare const __APP_VERSION__: string;
/** Время сборки, ISO-8601. В номер версии не входит: там дата тега, а не сборки. */
declare const __APP_BUILT_AT__: string;
