# Один вид перевода строки в истории независимо от системы разработчика.
# Без этого Windows и Linux правят одни и те же файлы целиком и каждый
# коммит выглядит как переписывание проекта.
* text=auto eol=lf
*.png binary
*.ico binary
